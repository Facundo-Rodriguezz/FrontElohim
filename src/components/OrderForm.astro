<section id="pedidos" class="pedidos-section">
  <h2 class="section-title">Realizar Pedido</h2>
  <form class="pedido-form">
    <div class="form-row">
      <div class="form-group">
        <label for="nombre">Nombre</label>
        <input type="text" id="nombre" name="nombre" placeholder="Escribe tu nombre completo" required />
      </div>
      <div class="form-group">
        <label for="telefono">Teléfono</label>
        <input type="tel" id="telefono" name="telefono" placeholder="Escribe tu número de teléfono" required />
      </div>
    </div>

    <div class="form-row">
      <div class="form-group">
        <label for="calle">Calle</label>
        <input type="text" id="calle" name="calle" placeholder="Escribe el nombre de la calle" required />
      </div>
      <div class="form-group">
        <label for="numeracion">Numeración</label>
        <input type="number" id="numeracion" name="numeracion" placeholder="Escribe el número de la dirección" required />
      </div>
    </div>

    <div class="form-row">
      <div class="form-group">
        <label for="piso">Piso (opcional)</label>
        <input type="text" id="piso" name="piso" placeholder="Escribe el piso, si aplica" />
      </div>
      <div class="form-group">
        <label for="departamento">Departamento (opcional)</label>
        <input type="text" id="departamento" name="departamento" placeholder="Escribe el departamento, si aplica" />
      </div>
    </div>

    <div class="form-row">
      <div class="form-group">
        <label for="localidad">Localidad</label>
        <input type="text" id="localidad" name="localidad" placeholder="Escribe la localidad" required />
      </div>
      <div class="form-group">
        <label for="provincia">Provincia</label>
        <input type="text" id="provincia" name="provincia" placeholder="Escribe la provincia" required />
      </div>
    </div>

    <div class="form-group">
      <label for="pedido">Detalle del pedido</label>
      <textarea id="pedido" name="pedido" rows="4" placeholder="Escribe el detalle del pedido (productos, cantidad, etc.)" required></textarea>
    </div>
    <button type="submit">Enviar Pedido</button>
  </form>
</section>

<script>
  document.querySelector('.pedido-form')?.addEventListener('submit', function (event) {
    event.preventDefault(); 

    
    const nombre = (document.getElementById('nombre') as HTMLInputElement).value.trim();
    const telefono = (document.getElementById('telefono') as HTMLInputElement).value.trim();
    const calle = (document.getElementById('calle') as HTMLInputElement).value.trim();
    const numeracion = (document.getElementById('numeracion') as HTMLInputElement).value.trim();
    const piso = (document.getElementById('piso') as HTMLInputElement).value.trim();
    const departamento = (document.getElementById('departamento') as HTMLInputElement).value.trim();
    const localidad = (document.getElementById('localidad') as HTMLInputElement).value.trim();
    const provincia = (document.getElementById('provincia') as HTMLInputElement).value.trim();
    const pedido = (document.getElementById('pedido') as HTMLTextAreaElement).value.trim();

    
    if (!pedido || pedido.length === 0) {
      alert("Por favor, escribe el detalle del pedido antes de enviarlo.");
      return;
    }

   
    let mensaje = `¡Hola! Quiero realizar un pedido:\n\n`;
    mensaje += `*Nombre:* ${nombre}\n`;
    mensaje += `*Teléfono:* ${telefono}\n`;
    mensaje += `*Dirección:* ${calle} ${numeracion}`;
    if (piso) mensaje += `, Piso: ${piso}`;
    if (departamento) mensaje += `, Dpto: ${departamento}`;
    mensaje += `\n*Localidad:* ${localidad}\n`;
    mensaje += `*Provincia:* ${provincia}\n`;
    mensaje += `*Pedido:* ${pedido}\n`; 

   
    const numeroWhatsApp = '5493516996607'; 

    
    const url = `https://wa.me/${numeroWhatsApp}?text=${encodeURIComponent(mensaje)}`;

    
    window.open(url, '_blank');
  });
</script>



<style>
  /* Variables de colores */
  :root {
    --burgundy: #8A1E41;
    --burgundy-light: #A83256;
    --gray-light: #f9f9f9;
    --gray-border: #ddd;
    --text-color: #333;
    --font-family: 'Poppins', sans-serif;
  }

  .pedidos-section {
    padding: 2rem 1rem;
    background-color: var(--gray-light);
    text-align: center;
  }

  .section-title {
    font-family: var(--font-family);
    color: var(--burgundy);
    font-size: 2rem;
    margin-bottom: 1.5rem;
  }

  .pedido-form {
    background: white;
    border-radius: 10px;
    padding: 2rem;
    margin: 0 auto;
    max-width: 600px;
    box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
  }

  .form-row {
    display: flex;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .form-group {
    flex: 1;
    display: flex;
    flex-direction: column;
  }

  label {
    margin-bottom: 0.5rem;
    color: var(--text-color);
    font-weight: bold;
    font-size: 1rem;
    font-family: var(--font-family);
  }

  input, textarea {
    padding: 0.75rem;
    border: 1px solid var(--gray-border);
    border-radius: 8px;
    font-size: 1rem;
    font-family: var(--font-family);
    color: var(--text-color);
    background: #fff;
    transition: border-color 0.3s ease;
  }

  input:focus, textarea:focus {
    border-color: var(--burgundy);
    outline: none;
  }

  textarea {
    resize: none;
    width: 100%;
  }

  button {
    width: 100%;
    padding: 1rem;
    background: var(--burgundy);
    color: white;
    border: none;
    border-radius: 8px;
    font-size: 1rem;
    font-weight: bold;
    font-family: var(--font-family);
    cursor: pointer;
    transition: background 0.3s ease;
  }

  button:hover {
    background: var(--burgundy-light);
  }

  button:active {
    background: var(--burgundy-light);
    transform: scale(0.98);
  }

  /* Responsividad */
  @media (max-width: 768px) {
        .pedido-form {
            padding: 1rem; /* Reducir padding del formulario */
        }

        .form-row {
            flex-direction: column; /* Cambiar a columna en móviles */
        }
    }

    .pedido-form {
      padding: 1.5rem;
    }

    .section-title {
      font-size: 1.5rem;
    }

    input, textarea {
      font-size: 0.9rem;
    }
  }
</style>
